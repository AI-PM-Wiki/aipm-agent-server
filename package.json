{
  "name": "aipm-agent-server",
  "version": "0.1.0",
  "private": true,
  "description": "AI-PM Wiki 文档问答 Agent 后端:BM25 检索 + Claude Agent SDK 回答,SSE 流式输出",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "smoke": "tsx src/smoke-search.ts",
    "cli": "tsx src/cli.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.241",
    "@anthropic-ai/sdk": "^0.120.0",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3"
  }
}
